plugin {
    id 3000006
    plugin Saltstack.Master.Minions.Check
    command check-saltmaster-minions
    datatype none
    category System
    netaccess yes
    prefer localhost
    description Verify salt-key and test.ping minions.
    abstract Verify that salt-key lists expected minions and try to test.ping them.
}
